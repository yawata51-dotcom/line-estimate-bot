name: Keep Render Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # 5分おきに実行

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render server
        run: curl -s https://line-estimate-bot.onrender.com/health
